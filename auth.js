async function digest(value){const bytes=new TextEncoder().encode(value);const hash=await crypto.subtle.digest('SHA-256',bytes);return Array.from(new Uint8Array(hash)).map(b=>b.toString(16).padStart(2,'0')).join('')}function unlock(){document.body.classList.remove('auth-locked')}if(sessionStorage.getItem('aliah-report-ok')==='1'){unlock()}document.getElementById('authForm')?.addEventListener('submit',async event=>{event.preventDefault();const user=document.getElementById('authUser').value.trim();const pass=document.getElementById('authPass').value;const error=document.getElementById('authError');const ok=user===window.reportAuth.user&&await digest(`${user}:${pass}`)===window.reportAuth.hash;if(ok){sessionStorage.setItem('aliah-report-ok','1');unlock()}else{error.textContent='Credenciales incorrectas.';document.getElementById('authPass').value=''}});